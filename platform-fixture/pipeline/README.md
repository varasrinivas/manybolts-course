# Pipeline fixtures

    bloated-suite/        the shared CI configuration and its timings
    cve-db.json           offline advisory database used by the supply-chain gate
    sbom/                 bills of materials per build, one per bolt
