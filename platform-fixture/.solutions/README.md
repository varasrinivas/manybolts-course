# Solutions and defect index

**Do not read this while working a lab.** It exists so an author can spot-check
the fixture and so a facilitator can mark.

Exclude this directory from the learner bundle:

    tar --exclude='.solutions' -czf platform-fixture.tgz platform-fixture/
