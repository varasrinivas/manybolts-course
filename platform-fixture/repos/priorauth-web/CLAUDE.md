# priorauth-web — repository steering

Inherits platform steering (`governance/STEERING.canonical.md`).

## Stack

React, ES modules, no build-time code generation.

## Conventions

Presentational components take data, not services. The queue renders reasons from
the clinical rules package so that the queue and the portal agree.
